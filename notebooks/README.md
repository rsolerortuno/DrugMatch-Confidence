# Notebooks

Notebooks are optional communication and exploration surfaces. They are not the source of truth.
All data preparation, training, evaluation, and prediction logic belongs in `src/drugmatch/` and is tested independently.
